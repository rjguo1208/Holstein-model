# DiagMC 理论页面验证记录

验证日期：2026-09-16。

- `npm run build` 成功渲染 76 处 LaTeX 表达式；每处均有 HTML 与 MathML 输出，未出现 KaTeX 解析错误。
- Tectonic 0.17.0 编译四份独立 TikZ 源文件，Poppler 转换为 SVG。图中的文字为矢量轮廓，不依赖访问者安装 LaTeX 字体。SVG 中保留了对应源文件的 SHA-256。
- `npm run check` 检查本地链接、目录锚点、数学字体、图的替代文字、矢量结构和源文件一致性。
- Chromium 在 1280、390、320 px 宽度下访问 `/Holstein-model/` 子路径；76 处公式和四幅图均加载成功，没有请求失败、字体失败或页面错误。
- 浏览器检查禁用了 JavaScript，并阻止外部资源请求。站点仍可完整阅读，没有向外部字体或脚本服务发出请求。
- 三个宽度下，页面整体宽度均未超出视口；窄屏上的长公式和宽图使用各自的横向滚动容器。检查了最窄视口下的公式左边界，无不可滚回的左侧裁切。
- 人工查看了桌面模型公式、单声子图、三种二阶配对，以及手机排版。生成了 8 页 A4 打印预览，并抽查公式段落。

这些是讲解网站与排版的检查。本站未实现或运行 DiagMC 数值计算，不把网站检查当作物理求解器的数值验证。
