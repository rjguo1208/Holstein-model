# 网站初版验证

检查日期：2026-09-07。这里验证网站与结果导出，不代表物理收敛认证。

- 数据覆盖 4 组参数、12 个动量点、72 组随机 LR 谱。有限空间容量校准保留 96 次优化的固定展宽与截断记录。
- 所有网页和归档 Markdown 的本地链接均有对应文件或锚点。
- 导出的 VED 原始谱前三个矩与解析求和规则一致，最大绝对误差 `1.599e-14`。
- JavaScript 对全部原始极点进行展宽，在 144 组设置上计算全谱和余谱 L¹，与已保存的 Python 结果相比，最大绝对差异 `7.050e-15`。
- 使用 Chromium `153.0.8010.12` 检查页面；24 次主参数切换显示的指标与源数据一致。
- 余谱、LR 样本量、度量截断、补充展宽和 CSV 下载可用。下载包含完整频率网格和两种方法的全谱与余谱。
- 检查 1440 px 桌面、390 px 和 320 px 手机布局，无页面横向溢出。
- 数据请求失败时有明确提示，重试可恢复；没有未处理的浏览器 JavaScript 错误。

核心检查可在仓库根目录重复执行：

```bash
python3 scripts/check_site.py
node --check site/assets/app.js
node scripts/check_spectra.mjs
```

浏览器仅访问本地预览服务器，网站没有加载外部运行时资源。网站部署工作流已提供，但没有启用或触发 Pages 发布。
