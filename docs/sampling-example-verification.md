# 采样实例与全站页面目录验证记录

日期：2026-09-19。

## 理论实例

固定一维、零温单电子模型，使用 t=ω₀=g=1、μ=−3、k=0、τ=2。
新内容在理论页第 5.1 节，包含三幅构型的连续示意、电子段表格、
图权重、插入/删除提议因子、随机接受判定和访问频率的解释。

- 从全部声子端点重新划分电子段，独立计算活动声子和、电子动量和传播作用量。
- 三个权重分别为 0.135335283237、0.030197383422、0.008651695203，
  与正文的 exp(−2)、exp(−3.5)、exp(−4.75) 一致。
- 两次插入的接受率分别为 0.446260320297 和 0.286504796860。
- 对每一对正反更新，比较 W×T×P；详细平衡在相对容差 1e−12 下成立。
- 把 μ 改为 −4 后重新计算，两个权重比保持不变，核对固定外部虚时间下的公共因子抵消。
- 时间提议采用两个独立均匀数排序，密度为 2/τ²；
  动量提议相对于 dq/(2π) 的密度为 1。
  删除时保留 1/(n+1) 的选线概率，零阶删除提议原地停留。
- 两次接受仅是手算示例。正文明确实际链还包含删除、拒绝和热化，
  测量必须计入拒绝后的重复构型，按 W 采样时访问次数不再额外乘 W。

## 页面目录与渲染

- 理论、数值结果、谱函数图三个页面具有相同的站点页面目录，
  使用相对链接和 aria-current 标明当前页面。
  理论页原有章节目录留在正文中，并增加直达实例的链接。
- 桌面宽度下页面目录固定在左侧；窄屏下转为页首导航；打印时隐藏导航。
- Node.js 22 下 npm run build 和 npm run check 通过：
  理论页 201 处、结果页 41 处、谱图页 25 处 LaTeX，共 267 处，
  均有 MathML；原有 4 幅 TikZ 图、16 幅科学图与本地链接检查通过。
- Chromium 在 1280、390、320 px 三种宽度检查全部三个页面，
  禁用页面 JavaScript 并阻止外部网络请求。
  无页面整体横向溢出、公式错误、资源加载失败或缺失图像。
- 实际点击站点目录跨页跳转，再从理论页章节目录跳到实例。
  检查每页恰有一个当前页标记，并验证桌面滚动后左侧目录仍固定。
- 目视检查桌面实例、构型示意、手机页面目录和权重表格；
  长公式、表格与示意图在自身容器内横向滚动。
  生成 A4 打印预览。

此更新的科学验证针对新增手算推导，没有重新运行生产 DiagMC 或谱重建。
